---
import Layout from '../layouts/Layout.astro';
import projects from '../services/projects.json'
---

<Layout title="immdaviid">

	<div class="text-base flex flex-col text-gray-300">
		<span class="text-2xl font-bold">David Valero</span>
		<span>
			Full Stack Developer.
			<br>
		</span>
	</div>
	
	<!-- <h2 class="mb-4 text-2xl font-bold text-gray-400">Latest projects</h2>
	<section>
		{
			projects.map(project =>{
				return(
				<div class="p-4 flex flex-row items-center gap-4">
					<h3 class="text-white font-semibold text-lg transition-all duration-150 hover:text-yellow-400">
						<a href={project.url}>{project.title}</a>		
					</h3>	
					<p class="text-gray-400">{project.description}</p>
					
				</div>
				)
			})
		}
	</section> -->
</Layout>